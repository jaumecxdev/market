<div class="form-check">
    <input class="form-check-input" type="checkbox" name="fixed" id="fixed" {{ $fixed ? 'checked' : '' }}>
    <label class="form-check-label" for="fixed">¿Este atributo siempre tiene el mismo valor para esta categoría de este Marketplace?</label>
</div>
