<div class="form-check">
    <input class="form-check-input" type="checkbox" name="leaf" id="leaf" {{ $leaf ? 'checked' : '' }}>
    <label class="form-check-label" for="leaf">Categoría Hoja, que se puede seleccionar en un producto</label>
</div>
